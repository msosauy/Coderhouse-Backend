<div>
    <h1>Hola, {{user.name}}</h1>

    {{#if isAdmin}}
        {{#each foods}}
            <div>
                <p>{{this.name}} - Precio {{this.price}}</p>
            </div>
        {{/each}}
    {{else}}
        <p>No tienes permisos para ver la lista de alimentos</p>
    {{/if}}
</div>
<script src="/js/index.js"></script>